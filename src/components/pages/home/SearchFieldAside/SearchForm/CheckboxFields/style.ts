'use client'

import styled from 'styled-components'

export const Container = styled.div`
  color: white;
  font-weight: 700;
  display: flex;
  gap: 16px;
  margin-bottom: 12px;
  opacity: 1;

  > label {
    opacity: 1;
    display: flex;
    align-items: center;
    gap: 8px;
  }
`
